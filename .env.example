TYPESAFE_API_KEY=replace_locally
# Optional: for the Luna comparison and downstream reader experiment.
OPENAI_API_KEY=replace_locally
# Optional: for the archived Qwen/OpenRouter protocol only.
OPENROUTER_API_KEY=replace_locally
